// Runtime configuration — injected before app.js loads.
// On Vercel: set this to your Railway backend URL.
// On local dev / Railway single-process: leave as empty string (same-origin).
window.API_BASE_URL = 'image-qa-app-exercise-production.up.railway.app';
